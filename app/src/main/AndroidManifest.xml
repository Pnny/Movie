<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuanchendu.movie">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.AddActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".ui.ShowAllActivity" />
        <activity android:name=".ui.RemoveActivity" />
        <activity android:name=".ui.DisplayMoviesByActorActivity" />
        <activity android:name=".ui.DisplayMoviesByGenreActivity" />
        <activity android:name=".ui.DisplayMovieHasNotSeeActivity" />
        <activity android:name=".ui.DisplayMoviesByNumberActivity" />
        <activity android:name=".ui.DisplayMoviesByAlphabeticallyActivity"></activity>
    </application>

</manifest>